{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "npm ci --prefix frontend && npm run build --prefix frontend && python manage.py collectstatic --noinput",
  "functions": {
    "api/index.py": {
      "includeFiles": "{frontend/dist/**,templates/**,static/**,staticfiles/**,.well-known/**}"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
